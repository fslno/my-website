---
description: Start the local preview server for the next.js project
---

1. Run `npm run dev` to start the development server. The project will run on port 9003 based on `package.json`.
// turbo
2. Open `http://localhost:9003` in your browser.
